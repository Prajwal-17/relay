# In memory for integration test
# M_VITE_DATABASE_URL=:memory:

M_VITE_MIGRATION_FOLDER=/drizzle

# optional development-runtime isolation for user-data directory
# M_VITE_USER_DATA_DIR=/absolute/path/to/relay-user-data

# api port - defaults to 4723 in development and 4722 in production.
# M_VITE_API_PORT=4723
