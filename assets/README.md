# Relay assets

This directory is the shared source of truth for the Relay app icon.

- `desktop/` contains the app icon SVGs and native Electron icons.
- `mobile/` contains the Android foreground/background and store icon assets.

Both desktop and mobile use the same charcoal background with the white Relay mark.
